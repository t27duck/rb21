# frozen_string_literal: true

RSpec.describe Rb21 do
  it "has a version number" do
    expect(Rb21::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
